#include <stdio.h>

/**
 * main - whatever
 * @argc: number of arguments
 * @argv: array of arguments
 *
 * Return: Always 0
*/

int main(int __attribute__((__unused__)) argc, char *argv[])
{
	printf("%d\n", argc);

	return (0);
}
